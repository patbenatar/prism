<% content_for :title, "GitHub isn't answering" %>

<div class="shell-narrow py-20">
  <%= render "shared/empty_state",
             icon: :prism,
             title: "GitHub isn't answering",
             body: "GitHub returned an error of its own, or didn't answer at all. Nothing on your side is wrong, and this is the one GitHub failure that usually fixes itself — so the link below is worth taking literally.",
             cta_label: @retry_label,
             cta_to: @retry_path %>
</div>
